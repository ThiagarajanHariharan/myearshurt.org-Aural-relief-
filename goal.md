# Current Macro-Goal

> **North star for the project.** The user defines the active milestone here. Agents align daily work in `tasks.md` to this goal.

---

## Active Milestone

**Make the whole AuralRelief site mobile-friendly** (`myearshurt.me` player + SEO landings)

Cursor orchestrates (inventory, acceptance, reviews, bus). Antigravity implements HTML/CSS/JS. Do not mix product diffs into the coordination-bus PR.

**Plan (source of truth):** `/cursor/stores/bc-010aff67-9608-48ff-84d1-805258b75a4e/docs/mobile-friendly-conversion-plan.md`

Reuse [PR #3](https://github.com/ThiagarajanHariharan/myearshurt.org-Aural-relief-/pull/3) drawer/subpage CSS (CUR-1/CUR-2) — those HTML changes are not on `main` yet.

---

## User-Defined Objective

**Objective:** Site-wide mobile conversion for Hariharan T — plan first, then tagged CUR vs AG execution.

**Success criteria:**
- [ ] Player (`index.html`): no horizontal overflow at 375px; 44px+ primary controls; safe-area; usable drawer tabs/sliders; audio unlock + resume
- [ ] All 16 canonical landing pages: wrapping headings, table scroll wrappers, `for-clinics.html` actually styled
- [ ] `{slug}/index.html` copies match canonical roots
- [ ] Notch DSP / HRTF orbit math unchanged
- [ ] Product work in separate PRs from [PR #4](https://github.com/ThiagarajanHariharan/myearshurt.org-Aural-relief-/pull/4) coordination bus

**Target:** Phased AG-2 → AG-11 as in the plan (no calendar estimate)

---

## Notes for Agents

- Break this goal into tagged tasks in `tasks.md` (already numbered CUR-3+ / AG-2+).
- If the goal changes, update this file first, then reconcile `tasks.md`.
- Do not mark work complete here; track completion in `tasks.md` and release locks in `status.md`.
- Cursor: no primary HTML/CSS/JS. Antigravity: lock files before product edits.
