# Current Macro-Goal

> **North star for the project.** The user defines the active milestone here. Agents align daily work in `tasks.md` to this goal.

---

## Active Milestone

**Establish dual-agent coordination and await user-defined milestone**

The dual-agent scaffold (`project.md`, `goal.md`, `tasks.md`, `status.md`) is in place. No product feature milestone is set yet.

---

## User-Defined Objective

<!-- Fill in below when ready. Example:

**Objective:** Add user-selectable notch frequency presets for common tinnitus bands.

**Success criteria:**
- [ ] Preset chips in the notch panel (2 kHz, 4 kHz, 8 kHz, custom)
- [ ] Presets update the biquad filter and response curve in real time
- [ ] No regression to spatial panning or timer fade-out

**Target:** (optional date or release tag)

-->

*(No user-defined objective yet — add your milestone in the section above.)*

---

## Notes for Agents

- Break this goal into tagged tasks in `tasks.md` once the user fills in the objective.
- If the goal changes, update this file first, then reconcile `tasks.md`.
- Do not mark work complete here; track completion in `tasks.md` and release locks in `status.md`.
